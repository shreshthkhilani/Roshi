<script type="text/javascript" src="logout.js"></script>

<div id="footer" class="container" align="center">
	<% if (signup) { %>
		<a href="/login" class="roshiLink">Login to your existing account</a>
	<% } %>
	<% if (signin) { %>
		<a href="/" class="roshiLink">Create a new account</a>
	<% } %>
	<% if (login) { %>
		<button type="button" class="btn btn-link roshiLink" id="logoutBtn">Logout</button>
	<% } %>
</div>